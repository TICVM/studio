
{
  "entities": {
    "Sector": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Sector",
      "type": "object",
      "description": "Represents a department or sector within the company, used to organize employees.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Sector entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the sector (e.g., 'Marketing', 'Financeiro')."
        },
        "ordem": {
          "type": "integer",
          "description": "Numeric priority for sorting sectors in the UI."
        },
        "subcategorias": {
          "type": "array",
          "items": { "type": "string" },
          "description": "List of subcategories associated with this sector."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the sector was created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "createdAt"
      ]
    },
    "Employee": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Employee",
      "type": "object",
      "description": "Represents a staff member within the company, associated with one sector.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Employee entity."
        },
        "fullName": {
          "type": "string",
          "description": "The full name of the employee."
        },
        "jobTitle": {
          "type": "string",
          "description": "The job title or position of the employee."
        },
        "email": {
          "type": "string",
          "description": "The email address of the employee.",
          "format": "email"
        },
        "ramal": {
          "type": "string",
          "description": "The internal phone extension/ramal."
        },
        "unidade": {
          "type": "string",
          "description": "The physical unit or branch where the employee works."
        },
        "photoUrl": {
          "type": "string",
          "description": "URL pointing to the employee's profile photo.",
          "format": "uri"
        },
        "setor_id": {
          "type": "string",
          "description": "Reference to the Sector the employee belongs to."
        },
        "subcategoria": {
          "type": "string",
          "description": "Subcategory within the sector."
        },
        "status": {
          "type": "string",
          "description": "The current employment status of the employee (e.g., 'active', 'inactive')."
        },
        "is_lider": {
          "type": "boolean",
          "description": "Indicates if the employee is a leader in their sector."
        },
        "titulo_lider": {
          "type": "string",
          "description": "Custom title for the leadership position (e.g., 'Coordenador', 'Diretor')."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the employee record was created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "fullName",
        "jobTitle",
        "photoUrl",
        "setor_id",
        "createdAt"
      ]
    },
    "AdministratorProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "AdministratorProfile",
      "type": "object",
      "description": "Represents an administrative user with privileges to manage sectors and employees.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the AdministratorProfile entity."
        },
        "fullName": {
          "type": "string",
          "description": "The full name of the administrator."
        },
        "email": {
          "type": "string",
          "description": "The email address of the administrator.",
          "format": "email"
        },
        "role": {
          "type": "string",
          "description": "The role of the user within the system."
        }
      },
      "required": [
        "id",
        "fullName",
        "email",
        "role"
      ]
    },
    "SystemSettings": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SystemSettings",
      "type": "object",
      "description": "Represents the system appearance and branding settings.",
      "properties": {
        "systemName": { "type": "string" },
        "logoUrl": { "type": "string" },
        "primaryColor": { "type": "string", "description": "Hex color code for the primary brand color." },
        "logoStyle": { 
          "type": "string", 
          "enum": ["square_with_name", "rectangular_no_name"],
          "description": "Defines if the logo is a square icon with name or a wide rectangular logo without name text."
        }
      }
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/admin_profiles/{adminId}",
        "definition": {
          "entityName": "AdministratorProfile",
          "schema": {
            "$ref": "#/backend/entities/AdministratorProfile"
          },
          "description": "Stores administrator profiles."
        }
      },
      {
        "path": "/sectors/{sectorId}",
        "definition": {
          "entityName": "Sector",
          "schema": {
            "$ref": "#/backend/entities/Sector"
          },
          "description": "Stores information about company sectors."
        }
      },
      {
        "path": "/employees/{employeeId}",
        "definition": {
          "entityName": "Employee",
          "schema": {
            "$ref": "#/backend/entities/Employee"
          },
          "description": "Stores employee records."
        }
      },
      {
        "path": "/settings/appearance",
        "definition": {
          "entityName": "SystemSettings",
          "schema": {
            "$ref": "#/backend/entities/SystemSettings"
          },
          "description": "Stores global system branding and settings."
        }
      }
    ]
  }
}
